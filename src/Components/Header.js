import React from 'react'
import styles from './Header.mudule.css'

const Header = () => {
    return (
        <div> className={styles.header}
            Header
        </div>
    )
}

export default Header
